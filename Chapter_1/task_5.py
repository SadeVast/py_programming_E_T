# We create a list that,
# when divided by 5,gives a remainder of 3
# and add an enumeration of numbers
nums = [(5 * k + 3) for k in range(10)]
# Output variable
print(nums)
# Output in reverse order
print(list(reversed(nums)))
